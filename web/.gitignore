node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
# verification-screenshot.png is committed on purpose: it's the README hero
# image (the atlas visible from the repo), regenerated by web/scripts/verify_map.py.
*.log
*.tsbuildinfo
